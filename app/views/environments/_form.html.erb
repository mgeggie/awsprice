<%= form_for(@environment) do |f| %>
  <% if @environment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@environment.errors.count, "error") %> prohibited this environment from being saved:</h2>

      <ul>
      <% @environment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :usage_rate %><br />
    <%= f.text_field :usage_rate %>
  </div>
  <div class="field">
    <%= f.label :outbound_bandwidth %><br />
    <%= f.text_field :outbound_bandwidth %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
